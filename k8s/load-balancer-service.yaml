apiVersion: v1
kind: Service
metadata:
  name: ip-rotator-lb-service
spec:
  selector:
    app: ip-rotator-loadbalancer
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
